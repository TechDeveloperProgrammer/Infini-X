
# Calamares Bootloader Module Configuration

--- # YAML header

# This module handles the installation of the bootloader (e.g., GRUB).
# No specific configuration needed here for a basic setup, as GRUB is handled by the ArchISO profile.

# Optional: Specify bootloader type (e.g., grub, systemd-boot)
# bootloaderType: "grub"

# Optional: Specify where to install the bootloader
# installLocation: "mbr"
